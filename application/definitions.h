#include <stdint.h>
#include <properties.c>
#define false 0
#define true 1

#define neighbourSend 1
#define aggregatorSend 1
#define supervisorSend 1
#define DEVICESTATE(x) (x)
#define DEVICEPROPERTIES(x) (x)
#define GRAPHPROPERTIES(x) (x)
#define MSG(x) (x)

void RTS(val);